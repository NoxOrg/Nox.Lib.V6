#
# CurrencyCacheBalance.entity.nox.yaml
#
# https://open.er-api.com/v6/latest/CHF
#
# yaml-language-server: $schema=https://noxorg.dev/schemas/Entity.json
#

name: CurrencyCacheBalance

description: The cache balance in Store

key:
  entities: [Store, Currency]

attributes:

  - name: Amount
    description: The amount
    type: decimal
    maxWidth: 14
    precision: 8