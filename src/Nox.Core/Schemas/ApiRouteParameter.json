{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "NOX Api Route Parameter",
  "description": "Defines NOX api route parameter",
  "type": "object",
  "additionalProperties": false,
  "required": ["name","type"],
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the route parameter"
    },
    "type": {
      "type": "string",
      "description": "The type of the route parameter"
    },
    "default": {
      "type": ["string", "integer", "boolean", "null", "number", "object", "array"],
      "description": "The default value for this parameter"
    },
    "minValue": {
      "type": "integer",
      "description": "The minimum value allowed for this parameter if any"
    },
    "maxValue": {
      "type": "integer",
      "description": "The maximum value allowed for this parameter if any"
    }
  }
}

